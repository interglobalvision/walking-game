Router.map(function() {

  // Scenes

  this.route('wakeup', {
    path: '/wakeup',
  });

  this.route('bedside', {
    path: '/bedside',
  });

  this.route('mirror', {
    path: '/mirror',
  });

});
